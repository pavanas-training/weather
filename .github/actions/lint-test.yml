name: lint test
description: Action to perform lint test

inputs:
  branch:
    description: tells when to run action
    type: string
runs:
  steps:
    - uses: actions/checkout@v2
    - name: Install ESLint
      run: npm install eslint
    - name: Run ESLint
      run: npm run lint

  